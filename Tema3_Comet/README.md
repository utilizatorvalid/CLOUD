# The Comet with Socket.io

Website: <>

```
        - long-pooling with socket event emit and reciving
        - the main Idea is the newsFetcher portal.
        - / => geting started with the news fetcher, 
            - emit subscribe event every 30 seconds
            - when a news apears all the connectic sockets will receive the event.
        - /publisher => the page where you can news, in order to share them. 
```




```
        - instructions for deploying

            - npm install 
            - npm start
            for local testing
            - localhost:3000
```
